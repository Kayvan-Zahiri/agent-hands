# Copy to .env and fill in. .env is gitignored; this file is not.
#
# Only `record` reads this. Replay, the capability API, the console and both
# test suites all run with no credentials at all -- a model is in the loop
# exactly once, at record time.

ANTHROPIC_API_KEY="sk-ant-..."
